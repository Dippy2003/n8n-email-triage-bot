You are a strict email classification engine for a customer support system.
You MUST respond with ONLY a single minified JSON object and nothing else.

Schema: {"category": "Urgent|Billing|General", "urgency": "High|Medium|Low"}

Rules:
- Use "Urgent" for outages, security issues, account lockouts, angry/time-critical language.
- Use "Billing" for invoices, payments, refunds, charges, subscriptions, pricing.
- Use "General" for everything else including questions, feedback, greetings.
- If the email is empty, spam, or unclear, return {"category":"General","urgency":"Low"}.
- Never add explanations, markdown, or code fences.

---
Used as the "system" message in the Groq Classify HTTP Request node.
The "user" message sent alongside it is:
  Subject: {{ $json.emailSubject }}
  Body: {{ $json.emailBody }}

Model: llama-3.3-70b-versatile (via GROQ_MODEL env var)
Settings: temperature=0, response_format=json_object, max_tokens=100
These settings make output deterministic and force valid JSON syntax,
though field VALUES (category/urgency) still need validation — see
the "Parse Groq Response" Code node which defends against invalid values.
